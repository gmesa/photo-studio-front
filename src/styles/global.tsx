import { GlobalStyles } from "@mui/material";

export const inputGlobalStyles = (
    <GlobalStyles
        styles={{
            '.container': {
                height: '72vh',
            },
            "*": {
            }
        }}
    />
);